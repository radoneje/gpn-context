doctype html
html
    head
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-88562228-1"></script>
    <link href="https://vjs.zencdn.net/7.8.4/video-js.css" rel="stylesheet"/>
    style.
        body{
            transition: opacity 1s ease;
            width: 100%;
            height: 100%;
            overflow: hidden;
            padding: 0;
            margin: 0;
        }
        .videoWrapper {
            width: 100%;
            position: relative;
            padding-bottom: 56.25%; /* 16:9 */
            height: 0;
        }
        .videoWrapper iframe, .videoWrapper video {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        #my-video {
            transition: opacity .5s ease;
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        .vjs-poster {
            background-size: cover;
        }

    body(style="opacity: 0;")
        .videoWrapper
            video(class="video video-js vjs-big-play-centered" id="my-video" style="opacity:1" controls playsinline preload="auto" width="640" height="360"  poster="/images/poster.jpg" data-setup="{}")
                p(class="vjs-no-js") Для просмотра видео, пожалуйста, обновите браузер, подробности по ссылке:<a href="https://videojs.com/html5-video-support/" target="_blank">supports HTML5 video</a></p>

    script(src="https://vjs.zencdn.net/7.8.4/video.js")
    script.
        window.dataLayer = window.dataLayer || [];
        function gtag() {
            dataLayer.push(arguments);
        }
        gtag('js', new Date());
        gtag('config', 'UA-88562228-1');

        var player = videojs('my-video');
        player.src("https://hls-fabrikanews.cdnvideo.ru/fabrikanews2/gpn/playlist.m3u8")
        document.body.style.opacity = 1;
        document.querySelector('video').style.opacity = 1;






